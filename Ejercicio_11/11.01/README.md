# Ejercicio 1 - Gestión de Perfil de Usuario (GET y POST con XMLHttpRequest)

Este proyecto muestra un perfil de usuario que se puede editar y guardar de forma asíncrona usando **XMLHttpRequest**.  
Se usó **Bootstrap 5** para los estilos.

## 🧩 Archivos principales
- `index.html` → interfaz del perfil  
- `main.js` → carga y envío de datos  
- `user_data.json` → datos del usuario  
- `styles.css` → estilos simples  

## 🚀 Cómo probar
1. Abre el proyecto con Live Server o un servidor local.  
2. Se cargan los datos desde `user_data.json`.  
3. Pulsa **Editar**, cambia los campos y luego **Guardar cambios**.  
4. Los datos se envían por **POST** a `https://webhook.site/` (usando proxy CORS).  

## 🌐 URL en Netlify
🔗 [https://dwec-11-01.netlify.app/]