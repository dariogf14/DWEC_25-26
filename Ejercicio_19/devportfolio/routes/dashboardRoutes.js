const express=require('express');const c=require('../controllers/dashboardController');const{requireAuth}=require('../middlewares/auth');const r=express.Router();r.use(requireAuth);r.get('/',c.dashboard);r.post('/profile',c.updateProfile);r.post('/social-links',c.addSocialLink);r.get('/social-links/delete/:id',c.deleteSocialLink);r.post('/projects',c.addProject);r.get('/projects/edit/:id',c.showEditProject);r.post('/projects/edit/:id',c.updateProject);r.get('/projects/delete/:id',c.deleteProject);module.exports=r;
